# NVIDIA Triton Inference Server

Production-ready inference serving platform supporting multiple frameworks (PyTorch, TensorFlow, ONNX, TensorRT). Provides dynamic batching, model versioning, ensemble pipelines, and GPU/CPU scheduling. Alternative runtime for complex model serving requirements.
