# Helm

Package manager for Kubernetes applications using templated YAML charts for reproducible deployments. Manages application lifecycle including installation, upgrades, rollbacks, and configuration. Used for deploying all services with environment-specific configurations.
