# kind (Kubernetes IN Docker)

Tool for running local Kubernetes clusters using Docker containers as cluster nodes. Enables rapid development and testing of Kubernetes applications without requiring a full cluster. Used for local development environment before production deployment.
