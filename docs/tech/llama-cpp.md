# llama.cpp

CPU and GPU inference engine for LLMs written in C++, optimized for running models on consumer hardware. Supports quantized models (GGUF format) for reduced memory footprint and faster inference. Alternative runtime for environments with limited GPU resources.
