# pgvector

PostgreSQL extension for vector similarity search supporting exact and approximate nearest neighbor queries. Stores embeddings alongside relational data enabling unified data management without separate vector database. Used for RAG retrieval and semantic search functionality.
