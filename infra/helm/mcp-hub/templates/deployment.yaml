apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "mcp-hub.fullname" . }}
  labels:
    {{- include "mcp-hub.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "mcp-hub.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "mcp-hub.selectorLabels" . | nindent 8 }}
    spec:
      containers:
      - name: mcp-hub
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        ports:
        - name: http
          containerPort: {{ .Values.service.port }}
          protocol: TCP
        env:
        - name: DATABASE_URL
          value: {{ .Values.config.databaseUrl | quote }}
        - name: MCP_SERVER_URL
          value: {{ .Values.config.mcpServerUrl | quote }}
        - name: MCP_CLIENT_URL
          value: {{ .Values.config.mcpClientUrl | quote }}
        - name: DEBUG
          value: {{ .Values.config.debug | quote }}
        resources:
          {{- toYaml .Values.resources | nindent 10 }}
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 5
          periodSeconds: 10
