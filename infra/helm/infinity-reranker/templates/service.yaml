apiVersion: v1
kind: Service
metadata:
  name: {{ include "infinity-reranker.fullname" . }}
  labels:
    {{- include "infinity-reranker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "infinity-reranker.selectorLabels" . | nindent 4 }}
