apiVersion: v1
kind: Service
metadata:
  name: {{ include "infinity-embeddings.fullname" . }}
  labels:
    {{- include "infinity-embeddings.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "infinity-embeddings.selectorLabels" . | nindent 4 }}
