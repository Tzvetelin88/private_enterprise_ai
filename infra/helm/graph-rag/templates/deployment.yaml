apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "graph-rag.fullname" . }}
  labels:
    {{- include "graph-rag.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "graph-rag.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "graph-rag.selectorLabels" . | nindent 8 }}
    spec:
      containers:
      - name: graph-rag
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        ports:
        - name: http
          containerPort: {{ .Values.service.port }}
          protocol: TCP
        env:
        - name: DATABASE_URL
          value: {{ .Values.config.databaseUrl | quote }}
        - name: NEO4J_URI
          value: {{ .Values.config.neo4jUri | quote }}
        - name: NEO4J_USER
          value: {{ .Values.config.neo4jUser | quote }}
        - name: NEO4J_PASSWORD
          value: {{ .Values.config.neo4jPassword | quote }}
        - name: INFINITY_EMBEDDINGS_URL
          value: {{ .Values.config.infinityEmbeddingsUrl | quote }}
        - name: EMBEDDING_MODEL
          value: {{ .Values.config.embeddingModel | quote }}
        - name: LLM_URL
          value: {{ .Values.config.llmUrl | quote }}
        - name: LLM_MODEL
          value: {{ .Values.config.llmModel | quote }}
        - name: TRAVERSAL_DEPTH
          value: {{ .Values.config.traversalDepth | quote }}
        - name: DEBUG
          value: {{ .Values.config.debug | quote }}
        resources:
          {{- toYaml .Values.resources | nindent 10 }}
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 15
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 10
          periodSeconds: 10
