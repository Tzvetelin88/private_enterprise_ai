apiVersion: v1
kind: Service
metadata:
  name: {{ include "graph-rag.fullname" . }}
  labels:
    {{- include "graph-rag.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "graph-rag.selectorLabels" . | nindent 4 }}
