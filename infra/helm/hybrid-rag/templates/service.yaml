apiVersion: v1
kind: Service
metadata:
  name: {{ include "hybrid-rag.fullname" . }}
  labels:
    {{- include "hybrid-rag.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hybrid-rag.selectorLabels" . | nindent 4 }}
