apiVersion: v1
kind: Service
metadata:
  name: {{ include "agentic-rag.fullname" . }}
  labels:
    {{- include "agentic-rag.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agentic-rag.selectorLabels" . | nindent 4 }}
